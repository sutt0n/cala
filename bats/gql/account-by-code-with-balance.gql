query accountWithBalance($code: String!, $journalId: UUID!, $currency: CurrencyCode!) {
  accountByCode(code: $code) {
    name
    balance(journalId: $journalId, currency: $currency) {
      settled {
        normalBalance {
          units
        }
      }
    }
  }
}
